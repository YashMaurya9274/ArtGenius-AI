import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  gradientHeader: {
    color: 'white',
    fontSize: 35,
    textAlign: 'center',
    marginTop: 20,
    fontWeight: '600',
  },
});

export default styles;
