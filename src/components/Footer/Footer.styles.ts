import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    marginTop: 'auto',
    marginBottom: 20,
    textAlign: 'center',
    color: 'gray',
  },
});

export default styles;
