import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  noTransactionsText: {
    color: 'gray',
    fontSize: 20,
    textAlign: 'center',
    marginTop: 30,
  },
});

export default styles;
